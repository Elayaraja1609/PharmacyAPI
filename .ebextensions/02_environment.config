# Environment variables will be set via AWS Console or EB CLI
# This file is a template - update values in AWS Console

option_settings:
  aws:elasticbeanstalk:application:environment:
    FLASK_ENV: "production"
    FLASK_DEBUG: "False"
    PORT: "5000"
    # MONGO_URI and JWT_SECRET_KEY should be set via AWS Console for security

